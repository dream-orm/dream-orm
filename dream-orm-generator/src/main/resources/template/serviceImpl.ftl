package ${serviceImplPackageName};

import com.dream.boot.impl.ServiceImpl;
import org.springframework.stereotype.Service;
import ${voClassName};
import ${boClassName};

/**
 * @Description: ${remark!''}
 * @Author: ${author!''}
 * @Date: ${dateTime!''}
 */
@Service
public class ${serviceImplName} extends ServiceImpl<${voName},${boName}> implements ${serviceName} {

}
